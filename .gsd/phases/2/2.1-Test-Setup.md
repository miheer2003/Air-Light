---
phase: 2
plan: 1
wave: 1
---

# Plan 2.1: Test Suite Setup

## Objective
Set up the automated testing framework (`pytest`) and write unit tests for the core logic components (`gesture_classifier.py` and `gesture_mapper.py`) to reduce technical debt.

## Context
- .gsd/SPEC.md
- .gsd/ARCHITECTURE.md
- file:///Users/miheer/Desktop/Air-Light/gesture_classifier.py
- file:///Users/miheer/Desktop/Air-Light/gesture_mapper.py
- file:///Users/miheer/Desktop/Air-Light/requirements.txt

## Tasks

<task type="auto">
  <name>Setup Pytest and Classifier Tests</name>
  <files>
    - /Users/miheer/Desktop/Air-Light/tests/test_gesture_classifier.py
    - /Users/miheer/Desktop/Air-Light/requirements.txt
  </files>
  <action>
    - Add `pytest` to `requirements.txt` (or dev dependencies if preferred).
    - Create `tests/test_gesture_classifier.py` to test the logic of `GestureClassifier.process_hand()`.
    - Mock MediaPipe landmark inputs to simulate 'Open Palm', 'Closed Fist', and 'Pinch'.
    - Assert that the classifier returns the correct gesture string and brightness value.
  </action>
  <verify>pytest tests/test_gesture_classifier.py -v</verify>
  <done>Pytest runs successfully and verifies the classifier behavior for at least 3 distinct gestures.</done>
</task>

<task type="auto">
  <name>Setup Mapper Tests</name>
  <files>
    - /Users/miheer/Desktop/Air-Light/tests/test_gesture_mapper.py
  </files>
  <action>
    - Create `tests/test_gesture_mapper.py` to test the logic of `GestureMapper.handle_gesture()`.
    - Mock `BulbController` so no actual network calls are made.
    - Test that passing 'Open Palm' calls `bulb.turn_on()`, 'Closed Fist' calls `bulb.turn_off()`, and 'Pinch' calls `bulb.set_brightness()`.
    - Ensure debouncing logic works by sending 'Open Palm' twice rapidly and verifying `bulb.turn_on()` is only called once.
  </action>
  <verify>pytest tests/test_gesture_mapper.py -v</verify>
  <done>Pytest runs successfully and verifies mapper logic and debouncing mechanism.</done>
</task>

## Success Criteria
- [ ] `pytest` is configured and installed.
- [ ] Both test files pass with 0 failures.
- [ ] Tests verify actual application logic, not just tautological assertions.
