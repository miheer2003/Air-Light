---
phase: 2
plan: 2
wave: 2
---

# Plan 2.2: Graceful Hardware Handling

## Objective
Prevent the application from crashing on startup if the webcam is unavailable. The UI should still launch and display an error message to the user.

## Context
- .gsd/SPEC.md
- file:///Users/miheer/Desktop/Air-Light/main.py
- file:///Users/miheer/Desktop/Air-Light/camera.py
- file:///Users/miheer/Desktop/Air-Light/ui.py

## Tasks

<task type="auto">
  <name>Graceful Camera Failure in Main</name>
  <files>
    - /Users/miheer/Desktop/Air-Light/main.py
    - /Users/miheer/Desktop/Air-Light/camera.py
  </files>
  <action>
    - In `main.py`, remove the `sys.exit(1)` when `camera.start()` fails.
    - Instead, set a flag (e.g., `self.camera_failed = True`) and allow the application to proceed to `self.ui.mainloop()`.
    - In `process_loop`, if `self.camera_failed` is True, do not attempt to read from the camera.
  </action>
  <verify>python -c "import main; print('Main parsed successfully.')"</verify>
  <done>Application does not exit when camera fails to start.</done>
</task>

<task type="auto">
  <name>Display Camera Error in UI</name>
  <files>
    - /Users/miheer/Desktop/Air-Light/main.py
    - /Users/miheer/Desktop/Air-Light/ui.py
  </files>
  <action>
    - In `main.py`, if the camera fails, call a method in `ui` to display an error state.
    - In `ui.py`, update `update_frame()` or create a `show_error()` method that displays text like "Camera Not Found" in the main video feed area instead of crashing.
  </action>
  <verify>python -c "import ui; print('UI parsed successfully.')"</verify>
  <done>The UI gracefully shows a missing camera message without throwing exceptions.</done>
</task>

## Success Criteria
- [ ] If no webcam is connected (or simulated failure), the app launches the GUI instead of crashing.
- [ ] The GUI clearly indicates that the camera is unavailable.
