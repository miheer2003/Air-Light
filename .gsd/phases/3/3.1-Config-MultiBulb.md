---
phase: 3
plan: 1
wave: 1
---

# Plan 3.1: Multi-Bulb Configuration

## Objective
Update `config.py` to support multiple bulbs by storing a list of device configurations instead of a single device configuration.

## Context
- .gsd/SPEC.md
- file:///Users/miheer/Desktop/Air-Light/config.py

## Tasks

<task type="auto">
  <name>Update AppConfig for Multiple Bulbs</name>
  <files>
    - /Users/miheer/Desktop/Air-Light/config.py
  </files>
  <action>
    - Remove `device_ip`, `device_id`, and `local_key` from `AppConfig`.
    - Add a new field `devices: list = None` to `AppConfig`.
    - In `AppConfig.__post_init__` or directly in the definition using a default factory, initialize `devices` with a list containing the default single device dictionary: `[{"ip": "192.168.1.100", "id": "your_device_id", "key": "your_local_key"}]`.
    - In `ConfigManager.load()`, if legacy fields (`device_ip`, `device_id`, `local_key`) are present in the JSON, migrate them to the new `devices` list structure before applying to the config.
  </action>
  <verify>python -c "import config; print('Config parsed successfully.')"</verify>
  <done>AppConfig can hold multiple devices and migrates legacy config data.</done>
</task>

## Success Criteria
- [ ] `config.py` handles a list of device configurations.
- [ ] Legacy configurations are migrated without crashing.
