---
phase: 3
plan: 2
wave: 2
---

# Plan 3.2: Multi-Bulb Controller

## Objective
Update `bulb_controller.py` to manage connections and broadcast commands to multiple bulbs simultaneously.

## Context
- .gsd/SPEC.md
- file:///Users/miheer/Desktop/Air-Light/bulb_controller.py
- file:///Users/miheer/Desktop/Air-Light/tests/test_gesture_mapper.py

## Tasks

<task type="auto">
  <name>Update BulbController to handle multiple devices</name>
  <files>
    - /Users/miheer/Desktop/Air-Light/bulb_controller.py
  </files>
  <action>
    - Replace the single `self.device` with a list `self.devices = []`.
    - In `connect()`, iterate through `config.devices`. For each device dict, instantiate a `tinytuya.BulbDevice` and append it to `self.devices` if connection succeeds.
    - Change `self.is_connected` to a property or method that returns `True` if at least one device is connected, or keep it as a boolean that represents overall readiness.
    - In `turn_on()`, `turn_off()`, `set_brightness()`, and `set_color()`, iterate through `self.devices` and apply the command to all of them.
  </action>
  <verify>python -c "import bulb_controller; print('Controller parsed successfully.')"</verify>
  <done>BulbController broadcasts commands to all configured devices.</done>
</task>

## Success Criteria
- [ ] `bulb_controller.py` maintains a list of Tuya devices.
- [ ] Commands like `turn_on` iterate through all devices.
