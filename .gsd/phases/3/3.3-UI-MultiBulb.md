---
phase: 3
plan: 3
wave: 3
---

# Plan 3.3: UI Updates for Multi-Bulb

## Objective
Update the UI to display the connection status of multiple bulbs.

## Context
- .gsd/SPEC.md
- file:///Users/miheer/Desktop/Air-Light/main.py
- file:///Users/miheer/Desktop/Air-Light/bulb_controller.py

## Tasks

<task type="auto">
  <name>Update Status Display</name>
  <files>
    - /Users/miheer/Desktop/Air-Light/main.py
    - /Users/miheer/Desktop/Air-Light/bulb_controller.py
  </files>
  <action>
    - In `bulb_controller.py`, add a property `connected_count` that returns the number of connected devices, and `total_count` that returns the total configured devices.
    - In `main.py`, update the "bulb" status text formatting in `process_loop()`. Instead of "Connected" / "Disconnected", show `f"{self.bulb.connected_count}/{self.bulb.total_count} Connected"`.
    - If `mock_mode` is active, display "MOCK MODE (N configured)".
  </action>
  <verify>python -c "import main; print('Main parsed successfully.')"</verify>
  <done>UI displays ratio of connected bulbs.</done>
</task>

## Success Criteria
- [ ] UI shows "X/Y Connected".
- [ ] Mock mode displays correctly.
